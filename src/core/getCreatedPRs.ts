import { ActivityQueryOptions, PullRequest } from "../types";

export async function getCreatedPRs(
  options: ActivityQueryOptions
): Promise<PullRequest[]> {
  const prs: PullRequest[] = [];

  return prs;
}
